metodo borrowing

.slice(inicio, fin (tenente antes de))
ej. let num = [1, 2, 3]
num.slice(0,2) --> // 1, 2 
(ya que 3 esta en la posicion 2 y este se detuvo antes de 2)

oninput ---> cada que se modifica el input
